import React, { useState } from 'react';

const Counter = () => {

    return (
        <div>
            <p>Counter: 0</p>
            <button>Add Number</button>
        </div>
    );
};

export default Counter;